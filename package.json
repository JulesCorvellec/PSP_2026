{
  "name": "psp-2026",
  "private": true,
  "version": "1.5.0",
  "description": "Outils de développement (tests) pour l'app PSP (index.html, sans build en production).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
